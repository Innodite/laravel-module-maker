/vendor/
/node_modules/
composer.lock
.phpunit.result.cache
.DS_Store
Thumbs.db

# Reportes de test (mantener estructura de carpetas)
docs/test-reports/*
!docs/test-reports/.gitkeep
!docs/test-reports/README.md

# Configuraciones temporales de PHPUnit
storage/phpunit-*.xml

# index.html, manual.html y assets/ (en la RAÍZ del repo) SÍ se versionan a propósito — son lo
# que se sube al subdominio. Solo el .zip generado por docs/publicar.sh queda fuera (binario
# derivado, se regenera).
/publicar.zip
