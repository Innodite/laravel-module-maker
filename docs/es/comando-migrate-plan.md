**Retirado.** Sigue registrado solo para decirlo: ejecutarlo devuelve error, no éxito.

Usar `innodite:deploy` — ver su ficha en **Los comandos**.

```bash
php artisan innodite:deploy stage --context=central
php artisan innodite:deploy stage --context=central --dry-run
```
