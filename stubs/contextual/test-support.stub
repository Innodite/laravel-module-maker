<?php

namespace {{{ namespace }}};

class {{{ className }}}Support
{
    public static function make(array $overrides = []): array
    {
        return array_merge([
            'name' => 'Test {{{ moduleName }}}',
        ], $overrides);
    }
}
